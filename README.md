# FastAPI E-commerce API

Полнофункциональный REST API для интернет-магазина, разработанный на FastAPI. Проект реализует современную архитектуру электронной коммерции с системой управления товарами, категориями, пользователями и отзывами.

## 📖 О проекте

Это backend-приложение для интернет-магазина, которое предоставляет полный набор инструментов для управления каталогом товаров и взаимодействия пользователей. Система поддерживает три типа пользователей: покупатели (buyers), продавцы (sellers) и администраторы (admins), каждый из которых имеет свой уровень доступа к функциональности.

### Ключевые возможности

- **Управление товарами** — продавцы могут создавать, редактировать и удалять свои товары с привязкой к категориям
- **Система отзывов** — покупатели оставляют отзывы с оценками (1-5), рейтинг товара пересчитывается автоматически
- **Категоризация** — гибкая система категорий для организации каталога
- **Безопасность** — JWT-аутентификация, ролевая модель доступа, защита операций
- **Производительность** — полностью асинхронная архитектура для высокой пропускной способности

## 🛠 Технологический стек

- **FastAPI** — современный асинхронный веб-фреймворк для Python
- **SQLAlchemy 2.0+** — ORM с поддержкой async/await
- **PostgreSQL** — надежная реляционная СУБД
- **asyncpg** — высокопроизводительный PostgreSQL драйвер
- **Alembic** — система миграций базы данных
- **Pydantic** — валидация и сериализация данных
- **JWT (PyJWT)** — токен-based аутентификация

## 🏗 Архитектура

Проект построен на принципах чистой архитектуры с разделением на слои:

- **Models** — SQLAlchemy модели, описывающие структуру БД
- **Repositories** — слой доступа к данным, изолирующий SQL-логику
- **Services** — бизнес-логика приложения (валидация, пересчет рейтингов, проверка прав)
- **Routers** — HTTP endpoints, обработка запросов
- **Schemas** — Pydantic модели для валидации входящих и исходящих данных

Такой подход обеспечивает легкость тестирования, масштабируемость и поддерживаемость кода.

## 🔐 Система ролей и прав доступа

**Buyer (Покупатель)**
- Просмотр каталога товаров
- Создание отзывов с оценками
- Удаление собственных отзывов

**Seller (Продавец)**
- Все возможности покупателя
- Создание и управление своими товарами
- Редактирование и удаление только своих товаров

**Admin (Администратор)**
- Полный доступ ко всем ресурсам
- Управление категориями
- Модерация отзывов (удаление любых)

## ⭐ Система отзывов и рейтингов

Одна из ключевых фич — умная система отзывов:
- Покупатели оставляют отзывы с текстом (опционально) и оценкой от 1 до 5
- При добавлении отзыва автоматически пересчитывается средний рейтинг товара
- При удалении отзыва рейтинг пересчитывается заново
- Используется мягкое удаление (soft delete) для сохранения истории

## 🚀 Быстрый старт

```bash
# Клонирование репозитория
git clone <repository-url>
cd fastapi_ecommerce

# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
# venv\Scripts\activate   # Windows

# Установка зависимостей
pip install -r requirements.txt

# Настройка переменных окружения
cp .env.example .env
# Отредактируйте .env с вашими настройками БД

# Применение миграций
alembic upgrade head

# Запуск сервера разработки
uvicorn app.main:app --reload
